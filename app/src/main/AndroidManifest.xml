<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demo.main">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|screenLayout|fontScale"
            android:theme="@style/TestStyle"
            android:name=".MainActivity" >
            <!--android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|fontScale|uiMode"-->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TabActivity"></activity>
        <activity android:name="com.demo.test.BaseTestPage"/>
        <activity android:name="com.demo.test.PageA"/>
        <activity android:name="com.demo.test.PageB"/>
        <activity android:name="com.demo.test.PageC"/>

        <service android:name="com.demo.main.MyService"/>
    </application>

</manifest>